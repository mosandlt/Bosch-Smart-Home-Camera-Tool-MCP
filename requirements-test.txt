pytest>=8.0
pytest-asyncio>=0.23
pytest-mock>=3.12
responses>=0.25
freezegun>=1.4
# Runtime deps mirrored from pyproject.toml [project.dependencies] so a
# fresh CI shell installing only requirements-test.txt can still import the
# package under test (TEST_DEPS_MIRROR_MANIFEST). Keep in sync on every
# dependency bump.
mcp>=1.2.0
pydantic>=2.6
requests>=2.31
urllib3>=2.0
httpx>=0.27
defusedxml>=0.7.1
